#include <cstdio>

namespace Fujiwara
{
using ll = long long;

static void solve()
{
    
}
static int Azusa()
{
    int t = 1;
    // scanf("%d", &t);
    while (t--)
        solve();
    return 0;
}
}  // namespace Fujiwara
int main()
{
    freopen("basic.in", "r", stdin);
    freopen("basic.out", "w", stdout);
    return Fujiwara::Azusa();
}
