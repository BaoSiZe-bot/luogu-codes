#include <string>
#include <iostream>
std::string t[200005], s;
int main()
{
    std::cin.tie(0) -> sync_with_stdio(0);
    int n;
    std::cin >> n;
    for (int i = 1; i <= n; ++i)
        std::cin >> t[i];
    std::cin >> s;
    return 0;
}
